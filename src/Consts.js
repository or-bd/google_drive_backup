const CREDENTIALS_PATH = `${__dirname}/../config/credentials.json`;
const TOKEN_PATH = `${__dirname}/../config/token.json`;
const SCOPES = ['https://www.googleapis.com/auth/drive'];

module.exports = {
  CREDENTIALS_PATH,
  TOKEN_PATH,
  SCOPES,
};
